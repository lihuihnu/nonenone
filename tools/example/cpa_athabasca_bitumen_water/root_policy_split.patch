diff --git a/models/include/natural/thermo/detail/three_phase_flash_core.inc b/models/include/natural/thermo/detail/three_phase_flash_core.inc
index 8a5f15a..9e5011f 100644
--- a/models/include/natural/thermo/detail/three_phase_flash_core.inc
+++ b/models/include/natural/thermo/detail/three_phase_flash_core.inc
@@ -107,12 +107,15 @@
     {
         if (eos_.usesCubicPlusAssociation())
         {
+            // Preserve active-root identity throughout residuals, Newton,
+            // canonicalization, and total-Gibbs evaluation. A locally lower
+            // G vapor root is a different candidate, not the same liquid.
             return eos_.phaseResult(
                 pressure,
                 temperature,
                 composition,
                 thermodynamicPhase,
-                options_.cpaSelectGibbsMinimumRoot);
+                false);
         }
 
         // 性能：一次 P–T flash 会重复计算逸度。PR 只有一套参数矩阵，SW 额外有一套水相矩阵；
@@ -140,6 +143,22 @@
             thermodynamicRole_(phase), pressure, temperature, composition);
     }
 
+    // Active phases retain their role-specific density root. Only a missing
+    // phase candidate may compare roots by Gibbs energy; never change the
+    // reference liquid branch while evaluating its stability.
+    [[nodiscard]] typename Eos::template PhaseResult<double>
+    stabilityCandidatePhaseResult_(
+        CompositionalPhase phase,
+        double pressure,
+        double temperature,
+        const Composition &composition) const
+    {
+        if (eos_.usesCubicPlusAssociation() && options_.cpaSelectGibbsMinimumRoot)
+            return eos_.phaseResult(pressure, temperature, composition,
+                                    thermodynamicRole_(phase), true);
+        return phaseResult_(phase, pressure, temperature, composition);
+    }
+
     void validatePT_(double pressure, double temperature) const
     {
         if (!(pressure > 0.0) || !std::isfinite(pressure))
diff --git a/models/include/natural/thermo/detail/three_phase_flash_restricted.inc b/models/include/natural/thermo/detail/three_phase_flash_restricted.inc
index 6316516..a1888a3 100644
--- a/models/include/natural/thermo/detail/three_phase_flash_restricted.inc
+++ b/models/include/natural/thermo/detail/three_phase_flash_restricted.inc
@@ -682,8 +682,26 @@
             const StabilityResult stability = stabilityTest(
                 pressure, temperature, z,
                 candidate.presence, candidate.composition);
-            if (!stability.valid || !stability.stable)
+            if (!stability.valid)
                 continue;
+            if (!stability.stable)
+            {
+                // A converged liquid branch is a seed, not an equilibrium
+                // certificate. Release its unstable missing phase and solve
+                // again with fixed active-root identities before comparing G.
+                if (!eos_.usesCubicPlusAssociation() ||
+                    !options_.cpaSelectGibbsMinimumRoot)
+                    continue;
+                candidate = equilibrateActiveSet_(
+                    pressure, temperature, z, candidate.presence, candidate);
+                if (!candidate.converged ||
+                    !validatePhysicalFlashResult_(pressure, temperature, z, candidate))
+                    continue;
+                const auto expandedStability = stabilityTest(
+                    pressure, temperature, z, candidate.presence, candidate.composition);
+                if (!expandedStability.valid || !expandedStability.stable)
+                    continue;
+            }
             const double g = dimensionlessGibbs_(
                 pressure, temperature, candidate);
             if (std::isfinite(g) && g < bestG)
diff --git a/models/include/natural/thermo/detail/three_phase_flash_stability.inc b/models/include/natural/thermo/detail/three_phase_flash_stability.inc
index fff1ef5..48ccb18 100644
--- a/models/include/natural/thermo/detail/three_phase_flash_stability.inc
+++ b/models/include/natural/thermo/detail/three_phase_flash_stability.inc
@@ -187,7 +187,7 @@
         else
             state.sum = std::exp(logSum);
 
-        const auto thermo = phaseResult_(
+        const auto thermo = stabilityCandidatePhaseResult_(
             candidate, pressure, temperature, state.composition);
         state.norm = 0.0;
         for (int i = 0; i < N; ++i)
@@ -254,7 +254,7 @@
             if (current.norm < options_.stabilityTolerance)
                 return finalizeStabilityTrial_(current, referenceComposition);
 
-            const auto thermo = phaseResult_(
+            const auto thermo = stabilityCandidatePhaseResult_(
                 candidate, pressure, temperature, current.composition);
             for (int i = 0; i < N; ++i)
             {
@@ -342,7 +342,7 @@
                 return {};
             if (current.norm < options_.stabilityTolerance)
                 return finalizeStabilityTrial_(current, referenceComposition);
-            const auto thermo = phaseResult_(
+            const auto thermo = stabilityCandidatePhaseResult_(
                 candidate, pressure, temperature, current.composition);
             for (int i = 0; i < N; ++i)
             {
diff --git a/models/include/natural/thermo/three_phase_flash_types.hpp b/models/include/natural/thermo/three_phase_flash_types.hpp
index dfa4c2b..2ca75e3 100644
--- a/models/include/natural/thermo/three_phase_flash_types.hpp
+++ b/models/include/natural/thermo/three_phase_flash_types.hpp
@@ -38,10 +38,10 @@ struct ThreePhaseFlashOptions
     double compositionFloor{1.0e-30};
     double logKStepLimit{2.0};
     double numericalJacobianStep{1.0e-6};
-    // CPA literature implementations such as Jia & Okuno (2018) select
-    // the lowest-Gibbs root when multiple density roots exist.  Natural's
-    // production default remains role-based liquid/vapor root selection;
-    // external reproduction benchmarks can opt into the literature rule.
+    // Opt-in CPA candidate-root comparison for missing-phase stability only.
+    // Active phases ALWAYS retain role-specific density roots: liquid for
+    // Oil/Water, vapor for Gas. This flag must never switch an active liquid
+    // to a vapor root while solving/continuing its equilibrium branch.
     bool cpaSelectGibbsMinimumRoot{false};
 };
 
diff --git a/tools/example/cpa_athabasca_bitumen_water/branch_consistency_probe.cpp b/tools/example/cpa_athabasca_bitumen_water/branch_consistency_probe.cpp
index bfc2d42..58bae04 100644
--- a/tools/example/cpa_athabasca_bitumen_water/branch_consistency_probe.cpp
+++ b/tools/example/cpa_athabasca_bitumen_water/branch_consistency_probe.cpp
@@ -35,7 +35,9 @@ ProbeCheck recordProbe(const Eos &eos, const Flash &flash,
             const auto role = static_cast<MPMC::CompositionalPhase>(k);
             if (!r.presence.contains(role)) continue;
             const auto &x = r.composition[k];
-            const auto selected = eos.phaseResult(p, t, x, role, true);
+            // Recompute on the stored role's fixed root, independently of
+            // the candidate-root Gibbs option used by stability searches.
+            const auto selected = eos.phaseResult(p, t, x, role, false);
             const auto liquid = eos.phaseResult(p, t, x, true, false);
             const auto vapor = eos.phaseResult(p, t, x, false, false);
             c.rootMismatch = std::max(c.rootMismatch,
@@ -136,9 +138,13 @@ int main(int argc, char **argv) {
             // A metastable OW branch is legitimate below a WLV boundary, but
             // it cannot validate OW equilibrium. Only require all paths to
             // agree when unrestricted equilibrium actually has two liquids.
-            const bool pass = cg.valid && cg.stable && (!owGlobal ||
-                (cc.valid && cc.stable && cw.valid && cw.stable &&
-                 gap <= 1.0e-6 && globalGap <= 1.0e-6));
+            // Even a metastable liquid branch must remain root-consistent,
+            // fugacity-closed and seed-independent. A global three-phase
+            // equilibrium must have no higher G than the restricted branch.
+            const bool pass = cg.valid && cg.stable && cc.valid && cw.valid &&
+                gap <= 1.0e-6 && cg.gibbsRT <= cc.gibbsRT + 1.0e-8 &&
+                cg.gibbsRT <= cw.gibbsRT + 1.0e-8 && (!owGlobal ||
+                (cc.stable && cw.stable && globalGap <= 1.0e-6));
             allPass = allPass && pass;
             gates << t << ',' << p << ',' << w << ',' << (pass ? "PASS" : "FAIL")
                 << ',' << gap << ',' << globalGap
